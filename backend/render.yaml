services:
  - type: web
    name: vehicle-tracking-backend
    runtime: node
    branch: main
    root: backend
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: PORT
        value: 10000
      - key: NODE_ENV
        value: production
    plan: free
